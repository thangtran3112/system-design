apiVersion: v1
kind: Service
metadata:
  name: users-service
  namespace: graphql-federation
spec:
  type: ClusterIP
  selector:
    app: users
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
