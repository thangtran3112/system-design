apiVersion: v1
kind: Service
metadata:
  name: todos-service
  namespace: graphql-federation
spec:
  type: ClusterIP
  selector:
    app: todos
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
