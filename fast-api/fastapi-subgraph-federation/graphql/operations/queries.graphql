# AUTO-GENERATED by scripts/generate-operations.mjs
# Do not edit manually. Regenerate with: npm run generate:ops

query Todos($ownerId: Int) {
  todos(ownerId: $ownerId) {
    id
    title
    description
    completed
    owner {
      id
      email
      name
      picture
      provider
    }
  }
}

query Todo($id: Int!) {
  todo(id: $id) {
    id
    title
    description
    completed
    owner {
      id
      email
      name
      picture
      provider
    }
  }
}

query Users {
  users {
    id
    todos {
      id
      title
      description
      completed
    }
    email
    name
    picture
    provider
  }
}

query User($id: Int!) {
  user(id: $id) {
    id
    todos {
      id
      title
      description
      completed
    }
    email
    name
    picture
    provider
  }
}
