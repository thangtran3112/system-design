apiVersion: apps/v1
kind: Deployment
metadata:
  name: todos
  namespace: graphql-federation
  labels:
    app: todos
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todos
  template:
    metadata:
      labels:
        app: todos
    spec:
      containers:
        - name: todos
          image: ACCOUNT_ID.dkr.ecr.us-east-1.amazonaws.com/graphql-federation-todos:latest
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: graphql-federation-config
          env:
            - name: TODOS_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: graphql-federation-secrets
                  key: TODOS_DATABASE_URL
          volumeMounts:
            - name: todos-data
              mountPath: /data
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 30
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
      volumes:
        - name: todos-data
          emptyDir: {}
